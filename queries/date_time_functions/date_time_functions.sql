SELECT 
	CURRENT_DATE,
	CURRENT_TIME,
	CURRENT_TIME(6),
	CURRENT_TIMESTAMP,
	CURRENT_TIMESTAMP(3);

SELECT 
	LOCALTIME,
	LOCALTIMESTAMP;

SELECT 
	now(), -- same as CURRENT_TIMESTAMP
	timeofday();

SELECT CURRENT_DATE + 1; -- tomorrow
